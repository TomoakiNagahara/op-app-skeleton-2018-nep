
<h1>HTTP Server port check</h1>
<code><pre>
sudo lsof -nP -iTCP:80 -sTCP:LISTEN
</pre></code>

<h1>PHP-FPM port check</h1>
<code><pre>
sudo lsof -nP -iTCP:9000 -sTCP:LISTEN
sudo lsof -Pni4 | grep LISTEN | grep php
</pre></code>
